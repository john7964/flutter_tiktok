import 'package:flutter/widgets.dart';
import 'package:message_view/message_view.dart';

class MessageViewIntegration extends StatelessWidget {
  const MessageViewIntegration({super.key});

  @override
  Widget build(BuildContext context) {
    return const MessageView();
  }
}
